import { DndMakedraggableDirective } from './dnd-makedraggable.directive';

describe('DndMakedraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new DndMakedraggableDirective();
    expect(directive).toBeTruthy();
  });
});
